# --- LLM provider configuration ---
# The LLM is part of the operator-note interpretation path.
# Set the key for at least one provider; the second is the fallback.

GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b

GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite

# Optional overrides
# LLM_TIMEOUT_SECONDS=25
# LLM_PRIMARY_MAX_RETRIES=1
# LLM_FALLBACK_MAX_RETRIES=1